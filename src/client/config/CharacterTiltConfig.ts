import { CharacterTiltConfigType } from "shared/types/ConfigTypes";

const CharacterTiltConfig: CharacterTiltConfigType = {
	Factor: new Vector2(0.1, 0.1),
	Responsiveness: 2.5,
};

export default CharacterTiltConfig;
